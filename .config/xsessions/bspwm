#!/bin/sh
# X properties
xsetroot -cursor_name left_ptr
xrdb -merge ~/.Xresources
nitrogen --restore

# background services
/usr/libexec/polkit-gnome-authentication-agent-1 &

# desktop services
sxhkd &
bspwmmon eDP1 &

# window manager
bspwm
while [ $? -eq 128 ]; do
	bspwm
done
