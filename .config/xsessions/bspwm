#!/bin/sh
# X properties
xsetroot -cursor_name left_ptr
xrdb -merge ~/.Xresources
nitrogen --restore

# background services
/usr/libexec/polkit-gnome-authentication-agent-1 &

# desktop services
barline | lemonbar -f terminus &
sxhkd &

# window manager
exec bspwm
