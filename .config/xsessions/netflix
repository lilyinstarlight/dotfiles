#!/bin/sh
# Disable DPMS and screen blanking
xset -dpms
xset s off

# Start the window manager
xfwm4 --replace --daemon --compositor=off

# Start Chrome in a different data directory in app mode for Netflix
exec google-chrome-stable --user-data-dir=".config/google-chrome-netflix" --kiosk "http://www.netflix.com/"
