#!/bin/sh
# desktop services
compton &

# TV detection
if xrandr | grep "DisplayPort-0 connected"; then
	if xrandr | grep "DisplayPort-2 connected"; then
		socup
	else
		tvup
	fi
else
	monup
fi

mkdir -p "$HOME"/.local/share/i3
exec i3 -d all &>"$HOME"/.local/share/i3/i3.log
