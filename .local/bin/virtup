#!/bin/sh
exec pkexec sh -c 'mount /mnt/vm && modprobe kvm_intel && modprobe openvswitch && systemctl start ovsdb-server && systemctl start ovs-vswitchd && systemctl start virtlogd && systemctl start libvirtd'
