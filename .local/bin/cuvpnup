#!/bin/sh
if [!  -e /mnt/crypt/foster ]; then
	zenity --error --text="Crypt not mounted."
fi

exec echo -e "$(pass sso/clemson)\npush" | pkexec /usr/sbin/openconnect -qbu fmclane https://cuvpn.clemson.edu/
