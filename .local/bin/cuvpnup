#!/bin/sh
exec echo -e "$(pass sso/clemson)\npush" | pkexec sh -c "rm /etc/resolv.conf && echo nameserver 127.0.0.53 >/etc/resolv.conf && /usr/sbin/openconnect -qbu fmclane https://cuvpn.clemson.edu/"
