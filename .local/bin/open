#!/bin/sh
IFS='|'

if [ "$#" -eq 0 ]; then
    files="$(zenity --file-selection --multiple --separator='|')"
else
    files="$*"
fi

for file in $files; do
    if [ -e "$file" ]; then
        run xdg-open "$file"
    else
        echo "open: $file: file not found" >&2
    fi
done
