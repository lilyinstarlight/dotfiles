#!/bin/sh
access="$(pass api/aws/cloud/access)"
secret="$(pass api/aws/cloud/secret)"
s3put -a "$access" -s "'$secret'" -b fkmclane -p "$HOME" "$HOME"/data
s3put -a "$access" -s "'$secret'" -b fkmclane -p "$HOME" "$HOME"/music
