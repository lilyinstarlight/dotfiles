polkit.addRule(function(action, subject) {
    if ((((action.id == "org.freedesktop.policykit.exec") && ((action.lookup("command_line") == "/usr/sbin/rfkill unblock bluetooth") || (action.lookup("command_line") == "/usr/sbin/rfkill block bluetooth") || (action.lookup("command_line") == "/sbin/modprobe hci_uart") || (action.lookup("command_line") == "/sbin/modprobe bnep") || (action.lookup("command_line") == "/sbin/modprobe hidp") || (action.lookup("command_line") == "/sbin/modprobe -r hci_uart bnep hidp"))) || ((action.id == "org.freedesktop.systemd1.manage-units") && action.lookup("unit") == "bluetooth.service")) && subject.isInGroup("wheel")) {
        return polkit.Result.YES;
    }
});
