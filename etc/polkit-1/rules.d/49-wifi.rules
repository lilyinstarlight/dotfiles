polkit.addRule(function(action, subject) {
    if ((((action.id == "org.freedesktop.policykit.exec") && ((action.lookup("command_line") == "/usr/sbin/rfkill unblock wifi") || (action.lookup("command_line") == "/usr/sbin/rfkill block wifi"))) || ((action.id == "org.freedesktop.systemd1.manage-units") && action.lookup("unit") == "wpa_supplicant@wlp3s0.service")) && subject.isInGroup("wheel")) {
        return polkit.Result.YES;
    }
});
