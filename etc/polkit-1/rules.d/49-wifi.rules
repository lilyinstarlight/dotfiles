polkit.addRule(function(action, subject) {
    if ((((action.id == "org.freedesktop.policykit.exec") && ((action.lookup("command_line") == "/usr/bin/rfkill unblock wifi") || (action.lookup("command_line") == "/usr/bin/rfkill block wifi"))) || ((action.id == "org.freedesktop.systemd1.manage-units") && (action.lookup("unit") == "wpa_supplicant@wlp8s0.service" || action.lookup("unit") == "wpa_supplicant@wlp0s20u2.service"))) && subject.isInGroup("wheel")) {
        return polkit.Result.YES;
    }
});
